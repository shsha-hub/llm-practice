# Day 4 미션 — LSTM·GRU

자기주도 실습 시간에 푼다. **난이도 3단계** 중 자기 자리를 골라 시작하자.
전부 다 할 필요는 없다. 🟢 만 끝내도 오늘 것은 익힌 셈이다.

| 단계 | 파일 | 무엇을 하나 | 목표 시간 |
|---|---|---|---|
| 🟢 **A. 관찰** | `A1_compare.py` | 세 모델을 직접 비교하고 **믿어도 되는지 판정** | 40분 |
| | `A2_playground.py` | RNN이 틀린 문장을 LSTM은 맞히나 (놀이터) | 40분 |
| 🟡 **B. 응용** | `B1_gru_TODO.py` | GRU 모델·훈련 루프를 **직접 채운다** (TODO 6개) | 50분 |
| | `B2_stacked_TODO.py` | 2층·dropout · `hidden` 뜯어보기 (TODO 5개) | 50분 |
| 🔴 **C. 도전** | `C_challenge_TODO.py` | 게이트 손구현 검증 · 게이트 값 관찰 · MAX_LEN 실험 | 60분+ |

정답: `solutions/` — **먼저 스스로 붙잡아 본 뒤에** 열자.

---

## 시작하기 전에

```bash
cd ~/p3-llm/c1-pytorch
source .venv/bin/activate
python missions/A1_compare.py          # 또는 VSCode에서 셀 단위 실행
```

- 미션 파일은 `day04/` 의 모듈(`model.py`·`textutils.py` 등)을 자동으로 찾아 쓴다.
- 🟢 A 는 **그대로 실행하면 돌아간다.** 채울 것은 표와 결론이다.
- 🟡 B · 🔴 C 는 `TODO` 를 채워야 돌아간다. 안 채우면 무엇을 채우라는 안내가 뜬다.

## 오늘 미션을 관통하는 한 가지

> ★ **차이(신호)가 재실행 흔들림(잡음)보다 뚜렷할 때만 "낫다"고 말한다.**

숫자 하나를 잘 뽑는 것보다, **그 숫자로 무엇을 말해도 되는지 아는 것**이 오늘의 목표다.
그래서 미션마다 "이 결과로 무엇을 주장할 수 있나"를 묻는다.

**"차이가 있다"와 "차이를 확인하지 못했다"는 다른 말이다.** 후자를 정직하게 쓰는 연습을 하자.

## 여러분 숫자가 자료와 달라도 정상이다

훈련에는 무작위가 섞인다. 시드를 고정해도 환경이 다르면 값이 달라진다.
**정확한 값을 맞히려 하지 말고 방향과 경향을 읽자.**

## 회고 때 가져올 것

- A-1 의 비교표 (모델 3종 · 평균 · 흔들림)
- A-2 에서 **두 모델의 답이 갈린 가장 재미있는 문장**
- B 를 했다면: 가장 오래 막혔던 지점과 어떻게 풀었는지
- C 를 했다면: `f=1, i=0` 일 때 무슨 일이 일어나는지 **말로** 설명
